# 🍽️ Restaurant Management System

## 📋 Описание проекта

Полнофункциональная система управления рестораном с REST API на FastAPI и современным веб-интерфейсом.

### ✨ Основные возможности

- 👥 **Управление сотрудниками** - полный CRUD для пользователей с ролевой системой
- 📋 **Управление заказами** - создание, редактирование, удаление заказов
- 🍕 **Управление меню** - добавление, редактирование блюд с категориями
- 🪑 **Управление столами** - отслеживание статусов столов (свободен/занят/зарезервирован)
- 🔐 **Аутентификация** - система входа с разными уровнями доступа
- 📊 **Отчеты** - визуализация данных о работе ресторана

### 🎭 Роли пользователей

- **Администратор** - полный доступ ко всем функциям
- **Официант** - управление заказами и столами
- **Повар** - просмотр заказов и обновление статусов

## 🏗️ Архитектура

```
proekt-master-websim-4.0------/
├── app/
│   ├── api/              # API эндпоинты
│   ├── database/         # Конфигурация БД
│   ├── models/           # SQLAlchemy модели
│   ├── repositories/     # Слой работы с БД
│   ├── schemas/          # Pydantic схемы
│   ├── services/         # Бизнес-логика
│   ├── static/           # CSS, JS, изображения
│   └── templates/        # HTML шаблоны
├── migrations/           # Alembic миграции
├── tests/                # Тесты
├── main.py               # Точка входа
└── requirements.txt      # Зависимости
```

## 🚀 Быстрый старт

### Требования

- Python 3.9+
- pip
- SQLite3

### Установка

1. Клонировать репозиторий:
```bash
git clone https://github.com/GrezinaVika/proekt-master-websim-4.0------
cd proekt-master-websim-4.0------
```

2. Переключиться на ветку night1712:
```bash
git checkout night1712
```

3. Установить зависимости:
```bash
pip install -r requirements.txt
```

4. Удалить старую базу данных (если существует):
```bash
rm -f restaurant.db
```

5. Запустить приложение:
```bash
python main.py
```

6. Открыть в браузере:
```
http://localhost:8000
```

## 📱 Использование

### Тестовые аккаунты

| Роль | Логин | Пароль |
|------|-------|--------|
| Официант | ofikNum1 | 123321 |
| Администратор | adminNum1 | 123321 |
| Повар | povarNum1 | 123321 |

### API Документация

После запуска доступна интерактивная документация:

- Swagger UI: `http://localhost:8000/api/docs`
- ReDoc: `http://localhost:8000/api/redoc`
- OpenAPI JSON: `http://localhost:8000/api/openapi.json`

### Основные API эндпоинты

#### Аутентификация
```bash
# Вход
POST /api/login
Body: {"username": "adminNum1", "password": "123321"}
```

#### Заказы
```bash
# Получить все заказы
GET /api/orders/

# Создать заказ
POST /api/orders/
Body: {"table_id": 1, "waiter_id": 1, "dishes": [], "total_amount": 0}

# Обновить заказ
PUT /api/orders/{order_id}
Body: {"status": "ready"}

# Удалить заказ
DELETE /api/orders/{order_id}
```

#### Блюда
```bash
# Получить все блюда
GET /api/dishes/

# Создать блюдо
POST /api/dishes/
Body: {"name": "Пицца", "price": 450.0, "category": "Основные блюда"}

# Обновить блюдо
PUT /api/dishes/{dish_id}

# Удалить блюдо
DELETE /api/dishes/{dish_id}
```

#### Столы
```bash
# Получить все столы
GET /api/tables/

# Обновить статус стола
PUT /api/tables/{table_id}
Body: {"status": "occupied"}
```

#### Сотрудники
```bash
# Получить всех сотрудников
GET /api/employees/

# Создать сотрудника
POST /api/employees/
Body: {"username": "user1", "password": "pass123", "name": "Иван Петров", "role": "waiter"}

# Обновить сотрудника
PUT /api/employees/{employee_id}

# Удалить сотрудника
DELETE /api/employees/{employee_id}
```

## 🛠️ Технологии

### Backend
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - встроенная база данных
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

### Frontend
- **HTML5** - структура страниц
- **CSS3** - стилизация с градиентами
- **JavaScript (ES6+)** - интерактивность
- **Fetch API** - работа с REST API

## 📚 Документация

В репозитории доступна расширенная документация:

- `FIXES_DETAILED.md` - подробное описание всех исправлений
- `FINAL_REPORT.txt` - итоговый отчет о выполнении
- `RELEASE_NOTES.md` - заметки о релизах

## ✅ Статус проекта

- ✅ База данных настроена и работает
- ✅ Все CRUD операции реализованы
- ✅ API полностью функционален
- ✅ Frontend интегрирован с backend
- ✅ Ролевая система работает
- ✅ Все основные функции протестированы

## 🐛 Известные исправления

Все критические ошибки исправлены:
- ✅ Проблемы с БД (no such column) - решено
- ✅ Столы не работали - исправлено
- ✅ Заказы нельзя было редактировать - исправлено
- ✅ Управление меню не работало - исправлено
- ✅ Раздел сотрудников не работал - исправлено
- ✅ Синтаксические ошибки JavaScript - исправлено
- ✅ Кнопки показывали только сообщения - исправлено

## 🔜 Планы развития

- [ ] Добавить unit-тесты
- [ ] Настроить GitHub Actions CI/CD
- [ ] Добавить Docker поддержку
- [ ] Улучшить систему аутентификации (JWT)
- [ ] Добавить кэширование
- [ ] Оптимизировать запросы к БД
- [ ] Развернуть на облачном сервере

## 👨‍💻 Разработка

### Структура кода

Проект следует принципам:
- **Repository Pattern** - разделение логики доступа к данным
- **Service Layer** - бизнес-логика изолирована от API
- **Clean Architecture** - слои не зависят друг от друга

### Добавление новых функций

1. Создать модель в `app/models/`
2. Создать схему в `app/schemas/`
3. Создать репозиторий в `app/repositories/`
4. Создать сервис в `app/services/`
5. Создать API эндпоинт в `app/api/`
6. Обновить frontend в `app/static/` и `app/templates/`

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👥 Автор

GrezinaVika

## 🙏 Благодарности

Спасибо всем, кто помогал в разработке и тестировании проекта!

---

**Дата последнего обновления:** 17 декабря 2025
**Версия:** 4.0
**Статус:** ✅ Готово к использованию
